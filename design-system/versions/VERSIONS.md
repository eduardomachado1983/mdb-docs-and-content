# Versões de marca

Cada linha é uma versão de tokens de marca aplicada sobre a mesma estrutura
(`tokens/core.json` + `platforms/`). Nenhuma versão altera a estrutura.

| Versão | Marca | Slug | Fonte de identidade | Status |
| ------ | ----- | ---- | -------------------- | ------ |
| —      | —     | —    | —                     | Nenhuma marca aplicada ainda |
