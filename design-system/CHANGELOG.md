# Changelog

## Unreleased

- Estrutura inicial do design system (`tokens/core.json`,
  `tokens/brand.template.json`, `platforms/desktop`, `platforms/mobile`).
- `tokens/core.json` populado com valores estruturais provisórios (a
  substituir pelos valores reais extraídos dos arquivos Figma macOS 27 /
  iOS 27 assim que estiverem acessíveis via API).
- Nenhum componente de plataforma ou marca aplicado ainda.

## v1.1.0 — Nature Health

- Primeira marca aplicada: `versions/nature-health/` (`brand.json` +
  `DESIGN.md` completo, fornecido pelo usuário).
- Paleta Marine/Forest/Feather/Sleek Grey, tipografia SF Pro, efeitos
  Liquid Glass, tom de voz pt-BR e regras de acessibilidade específicas do
  contexto de telemedicina.
- `tokens/core.json` e `platforms/` permanecem intocados — este release
  só adiciona uma pasta de versão de marca.
